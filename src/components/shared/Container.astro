---
interface Props {
  className?: string;
}
const { className } = Astro.props;
---

<main
  class={`max-w-screen-xl mx-auto px-8 md:px-10 py-4 lg:py-8 ${className} leading-tight`}
>
  <slot />
</main>
